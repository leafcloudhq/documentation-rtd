#Read Me Leafcloud tutorials

## Prerequisites

```
pip install mkdocs mkdocs-material
```

## Building
```
mkdocs build
```

this generates a directory called _site_ with a file called _index.html_ that you can open by accesing through the directory or opening in browser.

## Development

Using `mkdocs serve`, the built site is served on `localhost:8000` and rebuilt on changes

